/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["via.placeholder.com", "images.unsplash.com"], // Thêm domain được phép tải ảnh
  },
};

export default nextConfig;
